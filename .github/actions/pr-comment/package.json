{
	"name": "pr-comment",
	"private": true,
	"version": "1.0.0",
	"description": "Adds PR changes as a comment and labels files by extension",
	"main": "src/main.js",
	"scripts": {
		"prepare": "ncc build src/main.js -o dist"
	},
	"dependencies": {
		"@actions/core": "^1.10.1",
		"@actions/github": "^6.0.0"
	},
	"devDependencies": {
		"@vercel/ncc": "^0.38.1"
	}
}
